const Categorias = ({categoria}) => {
    return(
            <div className="col d-flex justify-content-evenly ">
                <div>
                    <h2 className="categorias">{categoria}</h2>
                </div>
            </div>
            
    )
};

export default Categorias;